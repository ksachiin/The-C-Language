#include <stdio.h>

int main() {
    int i = 1;

    // Using do-while loop to print numbers 1 to 10
    do {
        printf("%d ", i);
        i++;
    } while (i <= 10);

    printf("clanguage\n");  // Printing "clanguage" after the numbers
    return 0;
}
