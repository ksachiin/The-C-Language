#include <stdio.h>

int main() {
    // Using for loop to print numbers 10 to 1
    for (int i = 10; i >= 1; i--) {
        printf("%d ", i);
    }
    
    printf("\n");  // Adding a newline for better formatting
    return 0;
}
